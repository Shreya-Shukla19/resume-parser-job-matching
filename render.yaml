services:
  - type: web
    name: resume-parser-and-job-matcher
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
